@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model TaskManagement1.Models.AssignTaskModel


<div class="crud">
    <h2>Department Details</h2>
    <dl class="row">

        <dt class="col-sm-2">Task Id</dt>
        <dd class="col-sm-10">@Model.Taskid</dd>

        <dt class="col-sm-2">User Id</dt>
        <dd class="col-sm-10">@Model.UserId</dd>

        <dt class="col-sm-2">Submit Date</dt>
        <dd class="col-sm-10">@Model.SubmitDate</dd>

        <dt class="col-sm-2">Due Date</dt>
        <dd class="col-sm-10">@Model.DueDate</dd>

        <dt class="col-sm-2">Reamrk</dt>
        <dd class="col-sm-10">@Model.Remark</dd>
    </dl>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>